import React from 'react';
import GlobalView from '../views/global_view';

const Global = ({ navigation }) => (
  <GlobalView banner="World Information" navigation={navigation} />
);
export default Global;
