import React from 'react';
import CountryView from '../views/country_view';

const Countries = ({ navigation }) => (
  <CountryView banner="Country Wise Information" navigation={navigation} />
);

export default Countries;
